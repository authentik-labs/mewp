apiVersion: v1
kind: Service
metadata:
  name: mewp
  labels:
    app.kubernetes.io/name: mewp
spec:
  selector:
    app.kubernetes.io/name: mewp
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
